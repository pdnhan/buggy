# Buggy - Test Management for QA Teams
# Docker Compose for local development and self-hosted deployment

services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: buggy-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: "${POSTGRES_USER}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      POSTGRES_DB: "${POSTGRES_DB}"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Buggy Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: buggy-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public"
      AUTH_SECRET: "${AUTH_SECRET}"
      NEXTAUTH_URL: "${NEXTAUTH_URL:-http://localhost:3000}"
      AUTH_GOOGLE_ID: "${AUTH_GOOGLE_ID:-}"
      AUTH_GOOGLE_SECRET: "${AUTH_GOOGLE_SECRET:-}"
      AUTH_GITHUB_ID: "${AUTH_GITHUB_ID:-}"
      AUTH_GITHUB_SECRET: "${AUTH_GITHUB_SECRET:-}"
      ENABLE_SESSION_TESTING: "${ENABLE_SESSION_TESTING:-false}"
      ENABLE_RELEASE_TRACKING: "${ENABLE_RELEASE_TRACKING:-false}"
      # Number of trusted reverse-proxy hops in front of this app. This
      # compose file publishes the app port directly (no proxy in front), so
      # this must stay unset/0 here. If you put a reverse proxy in front of
      # this deployment, set it to the number of hops (usually 1) so
      # src/proxy.ts can trust X-Forwarded-For for per-client rate limiting;
      # left at 0, request rate limiting fails open per-client and falls
      # back to a coarse, shared circuit breaker (see CLAUDE.md).
      TRUST_PROXY_HOPS: "${TRUST_PROXY_HOPS:-0}"
    ports:
      - "3000:3000"

  # Database migrations (one-time setup)
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    container_name: buggy-migrate
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public"
    command: npx prisma db push
    restart: "no"

volumes:
  postgres_data:
